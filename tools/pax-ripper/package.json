{
  "name": "pax-ripper",
  "private": true,
  "type": "module",
  "scripts": {
    "rip": "bun run src/index.ts",
    "rip:presets": "bun run src/index.ts --presets",
    "rip:geometry": "bun run src/index.ts --geometry",
    "rip:flags": "bun run src/index.ts --flags",
    "rip:covers": "bun run src/index.ts --covers",
    "rip:preset": "bun run src/index.ts --preset",
    "rip:from-file": "bun run src/index.ts --from-file",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "playwright": "^1.40.0",
    "chalk": "^5.3.0"
  }
}
